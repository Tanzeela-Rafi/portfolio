# Tanzeela Rafi Portfolio

This folder is ready for GitHub Pages.

1. Upload every file and folder in this directory to the root of a GitHub repository.
2. Open the repository Settings, then Pages.
3. Choose Deploy from a branch, select the main branch and the root folder, then save.

The site uses relative asset paths, so the repository may have any name.
